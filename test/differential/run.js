// Differential test harness stub (plan §6.2).
// Runs the same operation sequence against the Rust server and a real
// CouchDB instance (docker-compose.yml in this folder), then diffs
// _changes feed / revision trees / _conflicts output.
//
// Not implemented yet — Phase 0 has no conflict handling to differential-test.
// Fill in once Phase 1 (revision trees) lands.

console.log("differential test harness: not yet implemented (see doc/roadmap.md Phase 1)");
